apiVersion: v1
kind: Service
metadata:
  name: oauth2-proxy
  namespace: artpulse
spec:
  selector:
    app: oauth2-proxy
  ports:
    - name: http
      port: 80
      targetPort: 4180
  type: ClusterIP
