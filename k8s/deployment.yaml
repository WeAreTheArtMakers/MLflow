apiVersion: apps/v1
kind: Deployment
metadata:
  name: artpulse
  namespace: artpulse
spec:
  replicas: 2
  selector:
    matchLabels:
      app: artpulse
  template:
    metadata:
      labels:
        app: artpulse
    spec:
      containers:
        - name: api
          image: artpulse:latest
          imagePullPolicy: IfNotPresent
          env:
            - name: PORT
              value: "8000"
            - name: MLFLOW_TRACKING_URI
              value: "file:/app/mlruns"
            - name: MLFLOW_EXPERIMENT_NAME
              value: "artpulse"
            - name: MODEL_URI_FILE
              value: "/app/artifacts/latest_model_uri.txt"
            # In a real cluster, prefer registry-based URIs:
            # - name: MODEL_URI
            #   value: "models:/artpulse/Production"
          ports:
            - containerPort: 8000
          readinessProbe:
            httpGet:
              path: /ready
              port: 8000
            initialDelaySeconds: 5
            periodSeconds: 5
          livenessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 10
            periodSeconds: 10
          resources:
            requests:
              cpu: "100m"
              memory: "256Mi"
            limits:
              cpu: "500m"
              memory: "512Mi"
